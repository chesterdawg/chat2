{
  "name": "ndis-chatbot",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.12"
  },
  "scripts": {
    "ci:docguard": "node ci/doc_guard.mjs",
    "lint": "eslint . || echo \"eslint not set up; skipping\"",
    "format": "prettier --check . || echo \"prettier not set up; skipping\"",
    "format:fix": "prettier --write . || true",
    "typecheck": "test -f tsconfig.json && tsc --noEmit || echo \"no tsconfig.json; skipping typecheck\"",
    "test": "vitest run || echo \"no tests yet\""
  },
  "devDependencies": {
    "eslint": "^9.9.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.2",
    "@types/node": "^20.14.10",
    "vitest": "^2.1.1"
  }
}
